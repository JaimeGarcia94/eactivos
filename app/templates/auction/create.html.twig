{% extends 'base.html.twig' %}

{% block title %}
    {% if edit is defined %}
        Editar subasta
    {% else %}
        Crear subasta
    {% endif %}


{% endblock %}

{% block body %}
<h1>
    {% if edit is defined%}
        Editar subasta
    {% else %}
        Crear subasta
    {% endif %}
</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}